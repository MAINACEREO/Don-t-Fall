{
  "name": "mini-terra-pro",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "android:copy": "vite build && npx cap copy android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "phaser": "^3.60.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "@capacitor/cli": "^5.0.0"
  }
}